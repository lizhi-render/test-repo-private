services:
- type: web
  name: next-js
  runtime: node
  plan: starter
  buildCommand: yarn; yarn build
  startCommand: yarn start
  envVars:
  - key: NODE_ENV
    value: production
  - key: BANANA
    value: chicken
  previews:
    generation: automatic
  autoDeploy: false

# - type: cron
#   name: next-js-CRONYWONYwww
#   runtime: node
#   plan: starter
#   schedule: "0 * * * *"
#   buildCommand: yarn; yarn build
#   startCommand: yarn start
#   envVars:
#   - key: NODE_ENV
#     value: production

